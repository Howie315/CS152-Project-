void func main() 
{
    int m;
    int n;
    n = 7;
    int o;
   
    if (n >= 100) 
    {
        defecate(n);

        if(n := 100)
        {
            defecate(n);
        }
        else
        {
            defecate(n);
        }
    } 
    else 
    {
        m = n - 1;
        defecate(m);
    }
    
    defecate(o);
}