int func add(int a, int b)
{
  return a + b;
}

int func mult(int a, int b)
{
  return a * b;
}

void func main()
{
  int a;
  int b;
  int c;
  int d;

  a = 100;
  b = 50;
  
  c = add(a, b);
  defecate(c);
  
  d = mult(c, a + b);
  defecate(d);
}
