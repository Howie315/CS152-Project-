void func main() {
    int m;
    int n;
    int o;
    
    $ main program $
    
    n = intake();
    
    if (n >= 100) {
        defecate(n);
    } else {
        m = n - 1;
        defecate(m);
    }
    
    defecate(o);
}
